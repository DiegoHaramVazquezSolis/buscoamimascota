import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container: {
        marginBottom: 16
    },
    card: {
        marginTop: 24,
        marginBottom: 48
    },
    actions: {
        fontSize: 16,
        fontWeight: '700'
    }
});