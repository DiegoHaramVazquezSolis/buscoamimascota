import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    indicator: {
        borderRadius: 100,
        height: 10,
        width: 10,
        marginLeft: 10
    }
});