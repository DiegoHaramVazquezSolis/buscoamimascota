import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    textInput: {
        width: '100%'
    },
    buttonContainer: {
        marginTop: 12,
        alignSelf: 'flex-end'
    }
});